## Installation
```
Git Clone from the Repo.
make new Directory.
copy The Full path of Directory and Paste it into the variable fullPath in certs_manual.
Add the slack webhook URL in certs_manual script.
write yous Domains in domains.txt file and follow the format in the file.
RUN certs_manual.sh script.
```
